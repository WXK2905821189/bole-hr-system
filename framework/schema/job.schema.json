{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hr:schema:job",
  "title": "Job",
  "description": "岗位/JD（参考 Frappe Job Opening，落到统一契约）",
  "type": "object",
  "required": ["jobId", "jobTitle", "meta"],
  "properties": {
    "jobId": { "type": "string" },
    "jobTitle": {
      "type": "string"
    },
    "recruiterId": {
      "type": "string",
      "description": "所属招聘者账号（不同招聘者管理不同岗位）"
    },
    "department": { "type": "string" },
    "location": { "type": "string" },
    "salary": { "type": "string", "description": "薪资范围（BOSS 同步的 salaryDesc）" },
    "experience": { "type": "string", "description": "经验要求（BOSS experienceName）" },
    "degree": { "type": "string", "description": "学历要求（BOSS degreeName）" },
    "jobType": { "type": "string", "description": "职位类型（BOSS jobTypeName，如 全职）" },
    "bossStatus": { "type": "string", "description": "BOSS 侧原始在招状态文字（在招 / 非在招），用于核对过滤是否准确" },
    "employmentType": { "type": "string", "enum": ["fulltime", "parttime", "contract", "intern"] },
    "status": { "type": "string", "enum": ["draft", "open", "closed", "archived"] },
    "jdRaw": { "type": "string" },
    "parsedKeywords": { "type": "array", "items": { "type": "string" } },
    "hardSkills": { "type": "array", "items": { "type": "string" } },
    "softSkills": { "type": "array", "items": { "type": "string" } },
    "meta": {
      "type": "object",
      "required": ["createdAt"],
      "properties": { "createdAt": { "type": "string", "format": "date-time" } }
    }
  }
}